sut:
  build: .
  command: ["test"]

  environment:
    - RAILS_ENV=test
    - DATABASE_URL=postgresql://postgres:postgres@db/test
    - MEMCACHED_URL=memcached

  links:
    - db
    - memcached

db:
  image: postgres:9.6

memcached:
  image: memcached:1.4.27
